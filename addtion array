/**
 * You can edit, run, and share this code.
 * play.kotlinlang.org
 */

// maximum element in array

fun main() {
    
    val arr=intArrayOf(10,20,30,40,50)
    var sum=0
    for(num in arr)
    {
        sum+=num
    }
    println(sum)
      
}
